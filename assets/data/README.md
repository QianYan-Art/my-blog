# Generated article index

`articles.json` is generated by `scripts/sync-kbase.js` from the private
`QianYan-KBase/public` tree.

It is intentionally ignored by Git so the public blog repository does not carry
private article metadata or source paths.
