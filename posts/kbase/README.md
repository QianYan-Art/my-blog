# Generated article pages

This directory is the output location for static article pages generated from
the private KBase repository.

`*.html` files here are ignored by Git. Run `npm run sync:kbase:local` locally,
or `/usr/local/bin/blog-sync-kbase.sh` on the server, to regenerate them.
